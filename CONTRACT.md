# Module contract

The per-module API of `@ashlr/lexicon`, in dependency order. Every module codes against `src/core/types.ts`. Import siblings only via `../core/index.js` (ESM, `.js` suffix on relative imports, NodeNext resolution). No default exports. Node >= 20, no `any` in public signatures. Update this file when a public signature changes.

## Module map (dependency order)

Each layer imports only from the layers above it, and siblings only through `src/core/index.js`.

```text
src/core/          types, schema, store, trust, matcher (+ stoplist), normalize, suggest, learn, stats, suggestTerms, harvest, importers/, exporters/, index
src/daemon/        clipboard-backends, clipboard                       <- core
src/serve/         config, server, index                               <- core
src/voice/         process, devices, models, recorder, transcribe, history, voice   <- core, daemon (clipboard write)
src/cli/           cli-entry, serve-paths, prompt, commands, cmd-import, cmd-install, cmd-trust, cmd-learn, cmd-review, cmd-suggest, cmd-serve, cmd-voice, cmd-setup, index   <- core, daemon, serve, voice
src/hooks/         user-prompt-submit                                   <- core
src/mcp/           server                                              <- core, cli (runDoctorReport, runInstall, runImport, runSetup)
plugin/            esbuild bundles of src/mcp/server.ts and src/hooks/user-prompt-submit.ts
extension/src/     browser extension; its core.ts bundles the browser-safe slice of src/core   <- core (build time only)
apps/macos/        LexiconBar, SwiftPM; talks to the CLI as a subprocess, never imports TypeScript
```


## src/core/types.ts
Dependency-free shared types.
- `TermCategory = 'brand' | 'person' | 'product' | 'acronym' | 'identifier' | 'place' | 'other'`, `TermScope = 'global' | 'project'`, `TermSource = 'user' | 'harvest:repo' | 'harvest:git' | 'harvest:package' | 'import' | 'learned'`.
- `Term { canonical; aliases: string[]; phonetic?; category?; caseSensitive?; scope?; source?; notes?; createdAt?; hits?; never?: string[] }`.
- `LexiconSettings { minConfidence?; phonetic?; fuzzy?; protectedWords?; skipCode? }`, `Lexicon { version: 1; terms: Term[]; settings? }`.
- `LexiconFile { path; scope; lexicon; exists }`, `LoadedLexicon { merged; global: LexiconFile; project?: LexiconFile; projectTrust?: 'trusted' | 'untrusted' | 'changed'; skippedProject?: LexiconFile }`.
- `MatchReason = 'alias' | 'phonetic' | 'fuzzy'`, `Replacement { start; end; original; replacement; canonical; reason; confidence }`, `NormalizeResult { input; output; replacements; changed }`, `NormalizeOptions { minConfidence?; phonetic?; fuzzy?; skipCode?; dryRun? }`.
- `HarvestCandidate { canonical; category; source; evidence: string[]; count; suggestedAliases }`, `HarvestOptions { limit?; minCount?; git?; packages?; identifiers?; ignore?: string[] }`.
- `ExportFormat` union of the 15 format names (see exporters), `ExportOptions { categories?: TermCategory[]; limit? }`.

## src/core/schema.ts
- `LexiconSchema`, `TermSchema`, `LexiconSettingsSchema`, `TermCategorySchema`, `TermScopeSchema`, `TermSourceSchema` (zod v4) mirroring types.ts.
- `parseLexicon(raw: unknown): Lexicon` throws a readable error on invalid input.
- `LIMITS`: `word` 80 (canonical, alias), `text` 200 (notes, phonetic), `aliases` 64 (aliases and `never` per term), `terms` 5000, `protectedWords` 1000. No control characters or line breaks in any field (including U+2028/U+2029 and NEL). `stripInvisible(s)` removes zero-width (U+200B to U+200F), bidi controls (U+202A to U+202E), bidi isolates (U+2066 to U+2069) and the BOM; `hasControlChars(s)`. `stripControlChars(s)` removes every control character (C0 incl. CR/LF/tab, DEL, C1 incl. NEL, U+2028/U+2029) plus everything `stripInvisible` removes, without truncating. `sanitizeForDisplay(s)` additionally strips whole ANSI escape sequences and every `\p{C}` code point and caps the result at `DISPLAY_MAX_CHARS` (200) with a trailing ellipsis; use it for anything printed to a terminal.
- `emptyLexicon(): Lexicon`.

## src/core/store.ts
- `PROJECT_FILE_NAME = '.lexicon.yaml'`, `MAX_LEXICON_BYTES` (2 MB; larger files are refused).
- `StoreOptions { cwd?; globalPath? }`, `LoadOptions extends StoreOptions { includeUntrusted? }`.
- `resolvePaths(opts?: StoreOptions): { global: string; project?: string }`
  - global: `$LEXICON_PATH` || `$XDG_CONFIG_HOME/lexicon/lexicon.yaml` || `~/.config/lexicon/lexicon.yaml` (or `opts.globalPath`).
  - project: walk up from cwd looking for `.lexicon.yaml` (stop at git root or `/`).
- `defaultProjectPath(cwd?): string` (git root, else cwd, joined with `.lexicon.yaml`).
- `readLexiconFile(path, scope): Promise<LexiconFile>` (missing file => exists:false, empty lexicon).
- `writeLexiconFile(file: LexiconFile): Promise<void>` (atomic write, mkdir -p, YAML with header comment).
- `loadLexicon(opts?: LoadOptions): Promise<LoadedLexicon>` merges global + project via `mergeLexicons(global, project?)`: project wins on canonical collision (case-insensitive), aliases unioned, settings follow the same precedence. The project file is merged only when trusted (see trust.ts) unless `includeUntrusted`; otherwise `skippedProject` holds it and `projectTrust` says why (`'untrusted' | 'changed'`).
- `addTerm(term: Term, opts?: StoreOptions & { scope?: TermScope; merge?: boolean }): Promise<{ file: LexiconFile; term: Term; created: boolean }>` merges aliases into an existing canonical when merge !== false.
- `removeTerm(canonical: string, opts?: StoreOptions & { scope?: TermScope }): Promise<boolean>`.
- `recordHits(canonicals: string[], opts?): Promise<void>` (best effort, never throws; project file consulted first so a hit is not double-counted in global).
- `findTerm(lexicon, canonical): Term | undefined` (case-insensitive), `dedupeAliases(aliases, canonical): string[]` (case-insensitive dedupe, drops the canonical itself).
- Trust gate for project-scope writes: `addTerm` (`scope: 'project'`) and `removeTerm` throw `ProjectTrustError` (`{ name: 'ProjectTrustError', path, status: 'untrusted' | 'changed' }`, message `project lexicon at <path> is untrusted | has changed since it was trusted; review it and run \`lexicon trust\` first, or write to the global lexicon instead`) when the target `.lexicon.yaml` exists and is not trusted, before reading it. A file that does not exist yet is created and registered as trusted; an already-trusted file is re-pinned after the write. `recordHits` silently skips an untrusted project file and never auto-trusts it.

## src/core/trust.ts
- Registry `<dirname(global)>/trust.json` (`TRUST_FILE_NAME`): `TrustRegistry { version: 1, trusted: { [absPath]: TrustEntry { sha256, trustedAt } } }`, keyed by symlink-resolved absolute path.
- `TrustStatus = 'trusted' | 'untrusted' | 'changed'`, `TrustListEntry extends TrustEntry { path; status: TrustStatus | 'missing' }`.
- `getTrustPath(opts?: StoreOptions): string`.
- `isTrusted(file: LexiconFile, opts?): Promise<TrustStatus>`: also 'trusted' when `LEXICON_TRUST_ALL=1` (`TRUST_ALL_ENV`, `trustAllEnabled()`), when the file is the global lexicon or sits directly in its directory (not deeper), or when it does not exist.
- `trustProject(path, opts?): Promise<TrustEntry>`, `untrustProject(path, opts?): Promise<boolean>`, `refreshTrust(path, opts?): Promise<boolean>` (re-pin only if already registered), `listTrusted(opts?): Promise<TrustListEntry[]>`.
- `readTrustRegistry(opts?)`, `writeTrustRegistry(registry, opts?)` (atomic), `hashFile(path): Promise<string | undefined>`.

## src/core/matcher.ts
Pure functions, no IO, no environment, no logging.
- `DEFAULT_MIN_CONFIDENCE = 0.82`, `STOPLIST: ReadonlySet<string>` (3376 words: common English words in their usual inflections plus everyday tech vocabulary; defined in `src/core/stoplist.ts` as a frozen Set whose `add`/`delete`/`clear` throw, re-exported here so the import path is unchanged). Product names that double as words (`docker`, `neon`, `whisper`) are deliberately absent.
- `buildIndex(lexicon: Lexicon): MatcherIndex` precomputes the alias map (`AliasEntry`, diacritics folded, `explicit` flag), phonetic keys (`PhoneticEntry`), token counts, `protectedWords` and per-term `hasExplicitAliases`.
- `findReplacements(text: string, index: MatcherIndex, opts?: NormalizeOptions): Replacement[]`
  - non-overlapping, sorted by start. Overlaps are resolved in two tiers: exact alias hits first (longest span, then earliest start), then phonetic/fuzzy hits that do not overlap an accepted span.
  - exact alias (word-boundary, multi-word, case-insensitive, diacritic-insensitive; also punctuation-stripped) => confidence 1. `caseSensitive` terms match case-sensitively and skip the phonetic pass.
  - phonetic: double metaphone of a token window equals that of an alias/canonical => confidence `0.9 * (1 - 0.25 * lenDiff/maxLen)` (`PHONETIC_LENGTH_WEIGHT`; the literal length formula rejected every headline case). Requires a key of >= 3 characters, a window of >= 4 letters unless the alias is that short; aliases whose every token is 1 or 2 letters ("j w t") get no key. A single-token window whose alpha form or the alias's alpha form starts with a vowel (`a e i o u y`) must start with the same letter (diacritics folded) or reach `similarity() >= 0.6` (`PHONETIC_LONE_TOKEN_MIN_SIM`) against that alias, since metaphone folds every initial vowel to `A` ("inter" / "entire" share `ANTR` at similarity 0.5). Two differing initial consonants and multi-token windows are exempt. A single-token window must also reach an edit similarity floor against the alias that depends on the key length: `>= 0.8` for a 3-character key (`PHONETIC_LONE_KEY3_MIN_SIM`; "lokus"/"locus" 0.8 passes, "lacks"/"locus" 0.6 does not), `>= 0.65` for a 4-character key (`PHONETIC_LONE_KEY4_MIN_SIM`; "playwrite"/"playwright" 0.7 passes, "inter"/"entire" 0.5 does not), none for 5 or more ("coopernetties"/"kubernetes" 0.46 matches). The floors apply whether or not the term has aliases; multi-token windows are exempt. `PhoneticEntry.alpha` holds the alias's alpha form.
  - fuzzy: normalized optimal-string-alignment (Damerau, adjacent transpositions) similarity >= threshold => confidence = similarity. Each candidate is bounded with `fastest-levenshtein` (bit-parallel) before the O(n*m) OSA distance runs, since OSA >= Levenshtein / 2.
  - inexact windows of 2+ tokens never start or end on a function word (articles, prepositions, conjunctions, pronouns except "her", auxiliaries, wh-words).
  - a single-token window matched against a term with explicit aliases needs confidence >= 0.88 (`ALIASED_PLAIN_WORD_MIN`): in the phonetic pass regardless of case ("Inter" -> Entire.io at 0.86 is refused, "Ashlur" -> Ashlr.AI at 0.90 passes); in the fuzzy pass only when the token is all lowercase ("Ashlet" -> Ashlr.AI at 0.83 still matches). The bar is `max(minConfidence, 0.88)`, so a lower `minConfidence` does not lower it.
  - a span already equal to a canonical (with or without a trailing possessive) is claimed during overlap resolution and omitted from the result.
  - a trailing possessive is preserved for every reason: the base view wins when it matches the same term or at least as confidently.
  - never rewrite: `settings.protectedWords` (blocks all passes including explicit aliases), `term.never` (blocks all passes for that term), the built-in `STOPLIST` (blocks phonetic/fuzzy only; explicit aliases win), text inside code spans/fences/URLs/emails when skipCode.
  - identifier glue (`GLUE_BEFORE` = `@ / # : \ ~ _ -`, `GLUE_AFTER` = `/ \ _`): a window whose first token is directly preceded by a `GLUE_BEFORE` character, or whose end is directly followed by a `GLUE_AFTER` character (no whitespace between), is skipped by every pass including the exact alias pass, whether or not skipCode is on (`@ashlr/lexicon`, `ashlrai/lexicon`, `ashlr_core`, `#ashlr`, `~/ashlr`, `C:\ashlr`). Only characters outside the window count, so a listed hyphenated alias (`ashler-ai`) matches as one window; a possessive `'s`, sentence punctuation and a hyphen after the window (`ashler- it`, `ashler - it`) are not glue. When skipCode is on, `PATH_RE` also treats any whitespace-free `scope/name` token containing `/` as a path even without a file extension (`ashlrai/lexicon`, `github.com/ashlrai/lexicon`, `/opt/ashlr`, and harmlessly `and/or`); `a / b` with spaces is prose.
  - never rewrite tokens shorter than 3 characters via fuzzy/phonetic (exact alias only).
- `phoneticKey(s: string): string` (double metaphone primary, alpha-only).
- `similarity(a: string, b: string): number` 0..1; optimal string alignment distance up to 40 characters, plain Levenshtein beyond, so its value does not depend on the caller.

## src/core/normalize.ts
- `normalize(text: string, lexicon: Lexicon, opts?: NormalizeOptions): NormalizeResult` applies findReplacements, preserves surrounding whitespace/punctuation, writes the canonical exactly as stored. `dryRun` returns `changed: false` with the candidate `replacements`; callers inspect `replacements.length`.
- `diffSummary(result: NormalizeResult): string` one line per replacement: `"Ashler" -> "Ashlr.AI" (alias, 1.00)`.

## src/core/suggest.ts
- `suggestAliases(canonical: string): string[]` generates likely STT misspellings: split camel/dots ("Ashlr.AI" -> "Ashlr AI", "Ashler", "Ashlar"), vowel insertions for consonant clusters, drop dots/hyphens, common phoneme confusions. Deterministic, <= 8 results, never returns the canonical itself.

## src/core/learn.ts
- `Correction { heard; meant }` (`heard` may be empty when the sentence only names the intended spelling; the caller fills it from context), `LearnResult { term; created; file; aliasAdded }`, `CanonicalSuggestion { term; confidence }`, `MAX_CORRECTION_LENGTH = 60`.
- `parseCorrection(text): Correction | undefined` table-driven detection; `CORRECTION_EXAMPLES` holds one example per pattern: "it's X, not Y" (also spelled/spelt/written/called), "I said X not Y", "I meant X not Y", `"X" not "Y"` (quoted only), "not Y, X", "replace Y with X", "Y -> X" (also `=>`, `→`), "that/this/it should be X", "Y should be X", "spelled X". Sides may be double-quoted, curly-quoted, backticked or bare (1 to 4 words); bare sides must look like a name for the `replace` and `Y should be` forms; a side made only of stoplist words is rejected.
- `learnCorrection(c: Correction, opts?: StoreOptions & { scope? }): Promise<LearnResult)` adds `heard` as an alias of the term whose canonical or alias equals `meant` (written to that term's file unless `scope` is given), else creates a term `meant` with `heard` plus `suggestAliases(meant)` and `source: 'learned'`. Throws on an empty side, sides over 60 characters, or identical spellings. No stoplist check: user intent wins.
- `suggestCanonicalFor(heard, lexicon): CanonicalSuggestion[]` top 3 existing terms by max(similarity, phonetic-key match ? 0.9 : 0), confidence >= 0.6, exact alias/canonical = 1.

## src/core/stats.ts
- `TOP_TERMS = 10`, `NEVER_HIT_CAP = 20`.
- `computeStats(loaded: LoadedLexicon): LexiconStats { termCount; aliasCount; totalHits; topTerms: { canonical; hits }[]; neverHit: string[]; byCategory; bySource; files: { path; scope; terms }[] }`.

## src/core/suggestTerms.ts
- `SuggestionKind = 'alias' | 'term' | 'never' | 'stale'`; `TermSuggestion { kind; canonical; alias?; reason; confidence (0..1); evidence: string[] (<= 5, each through sanitizeForDisplay and <= 120 chars); count; aliases?: string[] (for 'term': what to add with it); category? (for a harvest 'term') }`; `VoiceHistoryEntry { at; raw; output; model?; ms? }` (structurally accepts `voice/history.ts` `HistoryEntry`); `SuggestInput { loaded; history?; cwd?; limit? (default 20); now? (epoch ms for the stale age, tests) }`.
- `SUGGEST_DEFAULT_LIMIT = 20`, `STALE_AFTER_DAYS = 30`, `AUTO_APPLY_CONFIDENCE = 0.8` (what `lexicon suggest --yes` and any unattended caller apply).
- `voiceHistoryPath(globalPath)` = `<dirname(globalPath)>/voice/history.jsonl`; `loadVoiceHistory(globalPath?): Promise<VoiceHistoryEntry[]>` (default path from `resolvePaths()`, so `$LEXICON_PATH` applies) never throws: missing file, unreadable file and malformed lines yield nothing; keeps the newest 1000 entries.
- `suggestTerms(input): Promise<TermSuggestion[]>`; `history` defaults to `loadVoiceHistory(loaded.global.path)`. Identical `{ raw, output }` lines collapse into one with a count. Signals:
  - 'alias': (a) a 1..3 token window of `raw` (some non-stoplist token, no function word or bare number at an edge, never a lone stoplist word) that is still in `output`, is near an existing alias/canonical (edit similarity >= 0.7, or a shared double-metaphone key plus similarity >= 0.6/0.5/0 for keys of 3/4/5+ consonants), recurs >= 2 times: confidence `0.3 + 0.45 * score + 0.05 * min(count, 4)`; (b) a window the current matcher corrects by phonetic/fuzzy match >= 3 times: reason `the matcher would guess this as <canonical> (N times in history); make it an exact alias`, confidence `max(0.8, mean matcher confidence)` capped at 0.95; (c) a rewrite found by a token diff of `raw` vs `output` that reads as a canonical but that the current matcher no longer reproduces (and is not an ordinary word): confidence <= 0.75.
  - 'term': runs of capitalized / CamelCase tokens in `output` (not a lone word at a sentence start, not stoplist / HARVEST_STOPLIST, not a lexicon spelling nor a word of a multi-word canonical, not near an existing term, >= 4 letters) recurring >= 3 times: confidence `0.35 + 0.1 * min(count, 5)` capped at 0.9, `aliases = suggestAliases(canonical)`. With `cwd`, `harvestRepo(cwd, { limit: 10, minCount: 5 })` candidates not in the lexicon are added with the harvester's aliases and category, reason `seen N times in <dir> (harvest, <source>)`, confidence capped at 0.7 (below AUTO_APPLY_CONFIDENCE: repo identifiers are review material).
  - 'never': a rewrite in the diff whose original is a single ordinary word (lowercase, in STOPLIST or a function word) and that was not an explicit alias: `alias` is the word, reason starts `ordinary word rewritten by guess`, confidence 0.7 when the current matcher still guesses it, else `0.5 + 0.05 * min(count, 3)` (<= 0.65). Skipped when the term's `never` already lists it.
  - 'stale': `hits` 0/undefined, `createdAt` at least 30 days before `now`, and no spelling (canonical or alias, word by word) occurs in the history: reason `never matched in N days`, confidence 0.3, count 0. Terms without `createdAt` are never stale.
  - Dedupe key `(kind, canonical, alias)` case-insensitive: counts take the max (signals (a) and (b) may cite the same lines), confidence and reason follow the stronger one, evidence unions. Rank by `confidence * log(1 + max(count, 1))` descending, then confidence, then canonical; cap at `limit`.
  - Budget: under 100 ms for 1000 history lines and 200 terms (measured in tests/suggest-terms.test.ts; about 10 ms on an M-series Mac). The matcher runs only on lines that hold a near window or differ from their output; near-miss lookups are cached per window text.

## src/core/harvest.ts
- `harvestRepo(root: string, opts?: HarvestOptions): Promise<HarvestCandidate[]>`
  - package/module names (package.json name + deps, pyproject, Cargo.toml, go.mod)
  - PascalCase identifiers >= 2 humps from source files (ts/js/py/go/rs/java/rb/swift/kt), exported classes/types preferred
  - git log author names (`git log --format=%an`, 5s timeout), deduped
  - project dir name
  - README headings' proper nouns (capitalised tokens not at sentence start)
  - filters `HARVEST_STOPLIST` (README-word filter, distinct from matcher `STOPLIST` to avoid an `export *` collision in `core/index.ts`) and generic identifiers (String, Error, Component, ...).
  - ranks by count, returns candidates with `suggestedAliases` from suggest.ts.
  - never reads node_modules/dist/.git/vendor/build (plus `opts.ignore`); caps file size 512 KB; caps files scanned 5000.

## src/core/importers/index.ts
Pure, no IO.
- `ImportFormat = 'wispr' | 'superwhisper' | 'macos' | 'csv' | 'espanso' | 'text' | 'json' | 'auto'`, `ConcreteImportFormat = Exclude<ImportFormat, 'auto'>`.
- `IMPORT_FORMATS: readonly ImportFormat[]`, `IMPORT_FORMAT_INFO: Record<ImportFormat, { description }>`, `isImportFormat(value): value is ImportFormat`.
- `importLexicon(content: string, format: ImportFormat, opts?: ImportOptions { source?: TermSource; filename? }): ImportResult { terms: Term[]; format: ConcreteImportFormat; skipped: { line: number; reason: string }[] }` (`line` is 1-based; an entry index for JSON inputs). Rows are merged by `mergeRows(rows, source?)`: one term per canonical (case-insensitive, first spelling wins), aliases deduped case-insensitively, an alias equal to the canonical dropped, `source` defaults to `'import'`, blank canonicals reported in `skipped`. Throws a readable Error on unparseable input or an unknown format.
- `detectImportFormat(content, filename?): ConcreteImportFormat`: plist header -> macos; JSON array of `{original,replacement}` (or `{replacements}`) -> superwhisper; JSON/YAML with `terms` -> json; YAML `matches:` -> espanso; `word,replacement` header -> wispr; header containing `canonical` -> csv; then the extension (`.plist` -> macos, `.csv` -> wispr, `.yml`/`.yaml` -> espanso); else text. JSON of an unknown shape throws.
- One file per format, each returning `RawImport { rows: ImportRow[]; skipped: ImportSkip[] }` (`importers/shared.ts`): `parseWisprImport`, `parseSuperwhisperImport`, `parseMacosImport` (regex plist scanner, entities decoded), `parseCsvImport` (`canonical,alias,category,phonetic`, header-ordered, unknown category dropped), `parseEspansoImport` (skips `vars`, regex and multi-line matches; strips a leading `:`/`;` trigger prefix), `parseTextImport` (`Canonical`, `Canonical: a, b`, `Canonical = a | b`, `#` comments), `parseJsonImport` (parseLexicon; drops scope/createdAt/hits).
- `importers/csv-parse.ts`: `parseCsv(content): CsvRecord { line; fields }[]` (RFC 4180 quoting, CRLF, BOM), shared by wispr and csv.

## src/core/exporters/index.ts
- `exportLexicon(lexicon: Lexicon, format: ExportFormat, opts?: ExportOptions): string`.
- `EXPORT_FORMATS: readonly ExportFormat[]` (15), `EXPORT_FORMAT_INFO: Record<ExportFormat, { description; ext }>`, `isExportFormat(value)`.
- One file per format, each `export function export<Format>(lexicon, opts): string`, ordered by `sortByImportance()` from `shared.ts`: `wispr` (CSV word,replacement), `superwhisper` (replacements JSON), `whisper-prompt` (one line, `WHISPER_PROMPT_DEFAULT_LIMIT` 100 terms), `macos` (Text Replacement plist), `claude-md` (markdown table under `CLAUDE_MD_HEADING = '## Voice lexicon'`; `|` and every kind of line break escaped in cells), `csv` (canonical,alias), `json` (raw lexicon), `deepgram` (keywords with boost), `espanso` (match YAML), `assemblyai` (`{ word_boost, boost_param: "high" }`), `azure` (`{ phraseList }`), `google` (`{ adaptation: { phraseSets: [{ phrases: [{ value, boost }] }] } }`, boost 20 for brand/person/product else 10), `openai` (delegates to whisper-prompt), `text` (`Canonical: alias1, alias2`, sorted, round-trips with the text importer), `markdown` (`- **Canonical** (category): aliases`).
- `src/mcp/server.ts` keeps an `EXPORT_FORMAT_VALUES` tuple with a compile-time exhaustiveness check; extend it whenever `ExportFormat` grows.

## src/core/index.ts
Re-exports everything above (`types`, `schema`, `store`, `trust`, `matcher`, `normalize`, `harvest`, `exporters`, `importers`, `suggest`, `learn`, `stats`). The only cross-module import path; also the package entry point (`main` / `exports["."]`).

## src/daemon/clipboard-backends.ts
- `ClipboardBackend { name; description?; read(): Promise<string>; write(text): Promise<void> }`. `read` resolves `''` for an empty or non-text clipboard (the tool exits non-zero or prints nothing); a missing binary (`ENOENT`) or a display/session error still rejects.
- `ClipboardExec = (cmd, args, stdin?) => Promise<string>`; `defaultClipboardExec` spawns without a shell (writes ignore stdout and resolve on exit so `xclip -i`'s forked child cannot hold the pipe). `ExecError { cmd, exitCode, stderr }`.
- `CLIPBOARD_BACKEND_NAMES`, `ClipboardBackendName = 'pbcopy' | 'wl' | 'xclip' | 'xsel' | 'powershell'`, `isClipboardBackendName(v)`, `createClipboardBackend(name, exec?)`, and the factories `pbcopyBackend`, `wlBackend`, `xclipBackend`, `xselBackend`, `powershellBackend`: `pbpaste`/`pbcopy`; `wl-paste --no-newline`/`wl-copy`; `xclip -selection clipboard -o`/`-i`; `xsel --clipboard --output`/`--input`; `powershell -NoProfile -NonInteractive -Command` with `Get-Clipboard -Raw` written via `[Console]::Out.Write` (no trailing newline, UTF-8) and `Set-Clipboard -Value` of `[Console]::In.ReadToEnd()` from stdin. The PowerShell backend normalizes CRLF to `\n` on read and restores CRLF on the next write when the read had it.
- `detectClipboardBackend(platform?, env?, which?, exec?)`: darwin -> pbcopy (both binaries on PATH); linux/BSD -> wl when `WAYLAND_DISPLAY` is set and `wl-paste` + `wl-copy` exist, else xclip, else xsel, else throws with `LINUX_INSTALL_HINT` (`sudo apt install wl-clipboard` / `xclip`); win32 -> powershell (falls back to `pwsh`); anything else throws. `which` defaults to `findOnPath`.
- `findOnPath(bin, opts?: FindOnPathOptions { env?; platform?; exists? })`: splits `PATH` (`;` on win32, `:` elsewhere), on win32 tries each `PATHEXT` extension (default `.COM;.EXE;.BAT;.CMD`, upper then lower case) then the bare name, else requires `X_OK`. No process spawned.

## src/daemon/clipboard.ts
Clipboard watcher for macOS, Linux and Windows using the backends above. `ClipboardCommonOptions { dryRun?; quiet?; cwd?; read?; write?; backend?; platform?; env?; exec?; out?; err? }`; `resolveClipboard(opts)` prefers injected `read`/`write` over `backend`/detection (a single injected function is completed from the detected backend).
- `MAX_CLIPBOARD_CHARS = 20000`, `LEXICON_RELOAD_MS = 5000`, `PASTE_APPLESCRIPT`.
- `runClipboardDaemon(opts: ClipboardDaemonOptions { intervalMs?; signal? })` (loop): poll every 250ms (configurable), if text changed since last seen and normalize changes it, write back, print diff to stdout. Loop guard: remember last written value to avoid rewriting own output. Lexicon re-read at most every 5s. Graceful SIGINT/SIGTERM/`signal`.
- `runClipboardOnce(opts: ClipboardOnceOptions { paste?; sendPaste? }): Promise<ClipboardOnceResult { changed; written; corrections; pasted }>` (`--once`): read once, normalize, write back if changed (unless `dryRun`), print `N correction(s)` + diff or `no changes` (`quiet` silences). With `paste: true` on darwin runs `osascript -e 'tell application "System Events" to keystroke "v" using command down'` (injectable `sendPaste`) whether or not the text changed, and throws with an Accessibility hint if that fails; off darwin it prints a one-line notice to stderr and skips. Empty, oversized (> `MAX_CLIPBOARD_CHARS`) or letterless text is reported as no changes.
- `runDaemonCommand(opts: DaemonCommandOptions { once?; which? } & both option sets): Promise<number>`: `--which` prints `clipboard backend: <name> (<description>)` (+ `--paste` availability) and returns 0, or 1 with the detection error; `--paste` without `--once` throws; `--once` runs once; else loops. `parseBackendName(value)` validates `--backend`. `main(argv)` is the standalone entry. `pbpaste()` / `pbcopy()` remain for callers of the old macOS-only API.

## src/serve (config.ts, server.ts, index.ts) and src/cli/cmd-serve.ts
Local HTTP API for surfaces without hooks or MCP (browser extension, Claude Desktop, the Codex app, macOS Shortcuts, Raycast, the menu bar app). `node:http` only, no framework. Other agents code against this exactly; see `docs/LOCAL-API.md` for curl examples.
- `src/serve/config.ts`: `SERVE_FILE_NAME = 'serve.json'`, `DEFAULT_PORT = 41733` (fixed so clients can find it), `DEFAULT_HOST = '127.0.0.1'`, `EXTENSION_ORIGIN_PREFIXES` (`chrome-extension://`, `moz-extension://`, `safari-web-extension://`). `ServeConfig { port; token; createdAt; allowedOrigins? }` lives at `getServePath(opts?: StoreOptions)` = `<dirname(global lexicon)>/serve.json`, written atomically with mode 0600 (`writeServeConfig`). `ensureServeConfig(opts?)` creates it on first run (`port: 41733`, `token`: 32 random hex from `generateToken()`, `createdAt` ISO) and reuses it afterwards; `readServeConfig` returns undefined for a missing or unusable file (a malformed token is never reused). `isOriginAllowed(origin, allowedOrigins?)`: an extension origin or an exact match in `allowedOrigins`; `*` is never honoured. `serveUrl(port, host?)`, `isLoopbackHost(host)`.
- `src/serve/server.ts`: `createServer(opts?: ServeServerOptions { port?; host?; cwd?; globalPath?; quiet?; log?; version?; reloadMs?; maxConcurrent? }): LexiconHttpServer { server: http.Server; start(): Promise<ServeStartInfo { port; token; host; url; configPath }>; stop(): Promise<void> }`. `start()` ensures serve.json, binds `host:port` (default 127.0.0.1:41733; `port: 0` picks a free port), records the live port back into serve.json when it differs, and turns EADDRINUSE into a readable error. `stop()` closes every connection. No signal handling here (the CLI does it) so tests and the menu bar app can embed it. `MAX_BODY_BYTES` (1 MB), `MAX_CONCURRENT_REQUESTS` (64), `LEXICON_RELOAD_MS` (2000), `EXPORT_CONTENT_TYPES`, `exportContentType(format)`.
  - Auth: every request except `GET /health`, `GET /pair` and `OPTIONS` needs `Authorization: Bearer <token>` (constant-time compare); otherwise 401 `{ error }` with `WWW-Authenticate: Bearer`.
  - CORS: `OPTIONS` answers 204. `Access-Control-Allow-Origin: <origin>` (plus `Allow-Headers: Authorization, Content-Type`, `Allow-Methods: GET, POST`, `Vary: Origin`) is set only when `isOriginAllowed`; never `*`, no headers for any other origin.
  - Bodies are JSON objects; over 1 MB (declared or streamed) -> 413 after draining (never buffered, cut off at 16 MB); invalid JSON or a non-object -> 400; schema errors (zod) -> 400 `invalid request body: <field>: <message>`; unknown route -> 404; wrong method -> 405; `ProjectTrustError` -> 403; other input errors -> 400; anything else -> 500 `internal error` (detail logged). More than `maxConcurrent` in-flight requests -> 503 with `Retry-After: 1`. Every response carries `Cache-Control: no-store`.
  - `cwd` for project resolution: `cwd` in the request body (POST) or query string (GET) when given, else `opts.cwd` (the server's cwd). The lexicon is loaded through `loadLexicon({ cwd, globalPath })`, cached per cwd for `reloadMs` (2 s, at most 32 cwds) and dropped after every write. The trust gate applies exactly as elsewhere: an untrusted project file is never merged, and only its path is ever reported.
  - Endpoints (JSON in/out, `Content-Type: application/json; charset=utf-8`):
    - `GET /health[?cwd=]` -> `HealthResponse { ok: true, version, terms, projectTrust?, port }` (no auth).
    - `GET /pair` (`PAIR_PATH`) -> `text/html; charset=utf-8`, `pairPageHtml({ token, port, version })`: inline CSS, no script, no external request, `<meta name="lexicon-token">`, `lexicon-port`, `lexicon-version` (`PAIR_META`), `<p id="status">` for the extension to rewrite, `<div id="install">` revealed by a CSS animation after `PAIR_INSTALL_HINT_MS` (3 s). No bearer; instead 403 unless `isLoopbackAddress(req.socket.remoteAddress)` and `isPairHost(req.headers.host, boundPort)` (exactly `127.0.0.1:<port>` or `localhost:<port>`, case-insensitive). Extra headers `PAIR_HEADERS`: `X-Frame-Options: DENY`, `Content-Security-Policy: default-src 'none'; style-src 'unsafe-inline'`, `Referrer-Policy: no-referrer` (plus the usual `Cache-Control: no-store`). Non-GET -> 405.
    - `POST /normalize { text, dryRun?, minConfidence?, cwd? }` -> `NormalizeResponse` = `NormalizeResult & { summary }` (`diffSummary`, `''` when nothing matched). Records hits best-effort when `changed` and not `dryRun`.
    - `POST /learn { heard, meant, scope?, cwd? }` -> `LearnResponse { term, created, aliasAdded, path }` (`path` is the file written).
    - `POST /add { canonical, aliases?, phonetic?, category?, notes?, never?, scope?, cwd? }` -> `AddResponse { term, path, created }` (the MCP `add_term` shape; aliases omitted -> `suggestAliases`).
    - `GET /lexicon[?cwd=]` -> `LexiconResponse { lexicon: <merged Lexicon>, paths: { global, project? }, projectTrust?, skippedProject?: <path only> }`.
    - `GET /export/:format[?cwd=]` -> the export as text with the exporter's content type by `EXPORT_FORMAT_INFO[format].ext`: csv `text/csv`, json `application/json`, md `text/markdown`, txt `text/plain`, plist `application/xml`, yml `application/yaml` (all `; charset=utf-8`); unknown format -> 404.
    - `GET /stats[?cwd=]` -> `LexiconStats`.
  - Logs one line per request to `opts.log` (default stderr) unless `quiet`: `[serve] <METHOD> <path> <status> <ms>ms` (path sanitized).
- `src/serve/index.ts` re-exports both modules.
- `src/cli/cli-entry.ts` (no imports from the rest of the CLI): `PACKAGE_NAME`, `CLI_ENV_VAR = 'LEXICON_CLI'`, `whichBin(name, env?)` (re-exported by commands.ts), `findPackageRoot(fromUrlOrPath)` (walks up to the directory whose package.json is named `@ashlr/lexicon`; undefined otherwise), `resolveCliEntry(opts?: { cliPath?; env?; moduleUrl? }): string` = `cliPath` | `$LEXICON_CLI` | `<root>/dist/cli/index.js` when it exists (root from `moduleUrl`, default this module, so it is the same file from src/, dist/ and the plugin/ bundles) | a `lexicon` on PATH resolved through `realpathSync` and accepted only when it ends in `.js`/`.mjs`/`.cjs`, else a readable Error naming `LEXICON_CLI`. Existence is checked for the last two only.
- `src/cli/serve-paths.ts` (no filesystem): `LAUNCH_AGENT_LABEL = 'ai.ashlr.lexicon.serve'`, `SYSTEMD_UNIT_NAME = 'lexicon-serve.service'`, `SERVE_LABEL_ENV_VAR = 'LEXICON_SERVE_LABEL'`, `serveLabel(env?)` (the override when it matches `[A-Za-z0-9._-]+`, else the default), `launchAgentPath(home, env?)` = `~/Library/LaunchAgents/<label>.plist`, `launchAgentLogPath(home)`, `systemdUnitPath(home, env)` (honours `XDG_CONFIG_HOME`), `programPathFromPlist(text)` (second `ProgramArguments` string) and `programPathFromUnit(text)` (second quoted `ExecStart` word). cmd-serve.ts re-exports the names and paths.
- `src/cli/cmd-serve.ts`: `registerServeCommands(program, io)` adds `serve` (`--port <n>` 0..65535, `--host <host>`, `--json`, `--quiet`, `--show`, `--status`, `--pair`, `--install`, `--uninstall`); the modes are mutually exclusive. `runServe(opts: ServeOptions, io, deps?: ServeDeps { platform?; home?; env?; exec?: ServeExec; cliPath?; nodePath?; uid?; fetch?; signal?; onListening? })` dispatches to:
  - `runServeForeground`: `createServer` + `start()`, prints `lexicon serve listening on http://127.0.0.1:41733 (token in ~/.config/lexicon/serve.json)` (or a JSON line with `url, port, host, token, configPath` under `--json`), then waits for SIGINT/SIGTERM/`deps.signal`, `stop()`s and prints `lexicon serve stopped`. Never binds a non-loopback interface unless `--host` is given explicitly, and then prints a red `warning:` on stderr. A busy port returns 1 with `port N is already in use (... lexicon serve --status)`.
  - `runServeShow` (`--show`): `url:`, `token:`, `config:` lines (JSON: `{ url, port, token, configPath }`); `--port` overrides the recorded port.
  - `runServeStatus` (`--status`): GETs `/health` with a 1.5 s timeout (`STATUS_TIMEOUT_MS`); `lexicon serve is up at <url> (version X, N terms)` -> 0, `lexicon serve is down at <url> (<reason>)` plus a start hint -> 1 (JSON: `{ up, url, health? | reason? }`).
  - `runServePair` (`--pair`): GETs `/health` the same way; when down prints `lexicon serve is down at <url>` and `start it with \`lexicon serve\` or \`lexicon serve --install\` first` on stderr -> 1 (JSON `{ up: false, url, reason }`). When up runs `browserOpenCommand(url, platform)` through `deps.exec` (no shell): darwin `open <url>`, linux/freebsd/openbsd `xdg-open <url>`, win32 `cmd /c start "" <url>`, other platforms none; prints `opening <url> in your browser` -> 0, or `could not open a browser (<why>)` on stderr plus the URL on stdout -> 1 (JSON `{ up: true, url, opened, error? }`). `pairUrl(port)` = `http://127.0.0.1:<port>/pair`, always 127.0.0.1 (what the extension's match pattern and the server's Host check expect).
  - `runServeInstall` (`--install`) resolves the program with `resolveCliEntry({ env, cliPath: deps.cliPath? })` (a resolver error is printed and returns 1) and, on darwin/linux, refuses with `refusing to install the login service: <path> does not exist (...); nothing was written` (exit 1, no file written, no `bootout`) unless `fs.access` succeeds; then prints exactly what it wrote. darwin: `launchAgentPath(home, env)` (`serveLabel(env)`, `launchAgentPlist(nodePath, cliPath, logPath, label?)`: `ProgramArguments = [process.execPath, <abs dist/cli/index.js>, "serve"]`, `RunAtLoad`, `KeepAlive`, stdout/stderr to `~/Library/Logs/lexicon/serve.log`), then `launchctl bootout gui/$UID/<label>` (ignored), `launchctl bootstrap gui/$UID <plist>`, falling back to `launchctl load <plist>`. linux: `systemdUnitPath(home, env)` (`systemdUnit(nodePath, cliPath)`), then `systemctl --user daemon-reload` and `systemctl --user enable --now lexicon-serve.service`. win32: prints `schtasks /Create /SC ONLOGON ...` instructions, writes nothing, returns 0. Every command run is echoed as `ran: ...`; a failing final command returns 1 with the file left in place. `runServeUninstall` uses the same label.
  - `runServeUninstall` (`--uninstall`) reverses it: `launchctl bootout` (fallback `unload`) and remove the plist; `systemctl --user disable --now`, remove the unit, `daemon-reload`; on Windows prints the `schtasks /Delete` line.
  - `launchAgentPath(home)`, `launchAgentLogPath(home)`, `systemdUnitPath(home, env)`, `defaultServeExec` (spawn without a shell) are exported for tests.

## src/voice (process.ts, devices.ts, models.ts, recorder.ts, transcribe.ts, history.ts, voice.ts) and src/cli/cmd-voice.ts
Local push-to-talk dictation: ffmpeg (16 kHz mono s16 WAV) -> whisper.cpp (`--prompt` = `exportLexicon(merged, 'whisper-prompt')`) -> `normalize()` -> stdout / clipboard / Cmd+V. Every process interaction goes through injectable deps so tests never touch a microphone. See `docs/VOICE.md`.
- `src/voice/process.ts`: `VoiceExec = (cmd, args, opts?: ExecOptions { stdin?; timeoutMs? }) => Promise<ExecResult { code: number | null; stdout; stderr }>` (rejects only when the binary cannot start, e.g. `ENOENT`; a non-zero exit is a resolved result); `VoiceSpawn = (cmd, args, opts: SpawnOptions { detached; logFile? }) => ChildHandle { pid; exited: Promise<number | null>; stderr(); kill(signal) }` (a detached child gets its own group, stderr appended to `logFile`, and is unref'd). `defaultExec`, `defaultSpawn`, `defaultIsAlive(pid)` (signal 0; `EPERM` counts as alive), `defaultKill(pid, signal)`. `locateTool(names, opts?: LocateOptions { env?; platform?; extraDirs?; exists? })` searches PATH then `WELL_KNOWN_BIN_DIRS` (`/opt/homebrew/bin`, `/usr/local/bin`, `/usr/bin`; none on win32); `locateToolSync(names, env?, platform?)` for doctor. `locateWhisperCli(opts?)`: `LEXICON_WHISPER_BIN` (any name, e.g. `main`) else `WHISPER_BIN_NAMES` (`whisper-cli`, `whisper-cpp`); `locateFfmpeg(opts?)`: `LEXICON_FFMPEG_BIN` else `ffmpeg`. `INSTALL_HINTS` / `installHint(platform)`: `brew install ffmpeg whisper-cpp`, apt + build whisper.cpp, winget.
- `src/voice/devices.ts`: `AudioDevice { index; name; id; backend: 'avfoundation' | 'pulse' | 'alsa' | 'dshow' }`. Pure parsers `parseAvfoundationDevices(stderr)` (only the block after "AVFoundation audio devices:"), `parseDshowDevices(stderr)` (`"Name" (audio)` lines, older header form too), `parsePulseSources(stdout)` (`ffmpeg -sources pulse`; `.monitor` sources skipped), `parseArecordList(stdout)` (`card N: ... device M:` -> `hw:N,M`). `pickDevice(devices, selector)`: index, exact name or id, case-insensitive name, then substring. `listAudioDevices({ platform; ffmpeg; exec })` runs `ffmpeg -f avfoundation -list_devices true -i ""` (darwin), `ffmpeg -f dshow -list_devices true -i dummy` (win32), `ffmpeg -sources pulse` then `arecord -l` (elsewhere); ffmpeg's non-zero exit after listing is ignored. `formatDeviceList(devices)`.
- `src/voice/models.ts`: `DEFAULT_MODEL = 'base.en'`, `MODEL_BASE_URL` (`https://huggingface.co/ggerganov/whisper.cpp/resolve/main`), `REPO_MODELS_DIR` (`../../bench/audio/models/` from the module), `modelFileName(name)` = `ggml-<name>.bin`, `modelUrl(name)`, `modelsDir(globalPath, env?)` = `LEXICON_WHISPER_MODELS` or `<dirname(globalPath)>/models`. `isModelPath(spec)` (ends in `.bin`, contains a separator, starts with `.` or `~`). `resolveModel(spec, opts: ResolveModelOptions { globalPath; env?; fallbackDirs?; exists? }): ResolvedModel { name; path; present; foundIn?; url? }`: a path is used as-is; a name is looked up in the models dir, then `fallbackDirs` (default the repo models). `ensureModel(spec, opts: EnsureModelOptions { download?: Downloader; log?; progress? })` downloads a missing named model (`Downloader = (url, dest, onProgress: (DownloadProgress { received; total? }) => void) => Promise<void>`; `defaultDownloader` uses `fetch`, streams to `<dest>.part`, renames) and throws for a missing explicit path. `formatProgress(name, p)`.
- `src/voice/recorder.ts`: `voiceDir(globalPath)` = `<dirname(globalPath)>/voice`; `stateFilePath` = `voice/recording.json`; `RecordingState { pid; wav; startedAt }` with `readState` (undefined for missing/malformed), `writeState` (atomic), `clearState`. `resolveInput({ platform; ffmpeg; exec; device?; devices? }): InputSpec { format; input }`: darwin `avfoundation` `:default` / `:N` / `:<name>`; linux `pulse` `default` when `ffmpeg -devices` lists pulse, else `alsa` `default`; win32 `dshow` `audio=<name>` (first listed device when `--device` is absent; throws when none). `ffmpegRecordArgs({ input; wav; seconds? })` = `-nostdin -hide_banner -loglevel error -f <format> -i <input> [-t <s>] -ac 1 -ar 16000 -c:a pcm_s16le -y <wav>`. `startRecorder({ ffmpeg; spawn; input; wav; seconds?; detached; logFile? })`. `stopRecorder({ pid; isAlive; kill; sleep?; graceMs?; alreadySignalled? }): StopResult { finalized; killed }`: SIGINT (skipped when `alreadySignalled`, i.e. Ctrl-C already reached ffmpeg), poll until the pid is gone or `STOP_GRACE_MS` (3000) elapses, then SIGKILL. `MAX_TOGGLE_SECONDS = 600` (a toggle recording passes `-t`). `wavHasAudio(wav)`: size > 44 bytes.
- `src/voice/transcribe.ts`: `cleanTranscript(text)`: drops `[...]` tags always, `(...)`/`*...*` only when one to three words that look like a noise annotation (`(applause)`, `(soft music)`, `*laughs*`), collapses whitespace, trims. `whisperArgs({ modelPath; wav; outBase; lang; translate; prompt? })` = `-m <model> -f <wav> -l <lang> -nt -np -oj -of <outBase> [-tr] [--prompt <p>]`. `parseWhisperOutput(json?, stdout)` joins `transcription[].text`, falls back to stdout. `buildWhisperPrompt(lexicon)` = `exportLexicon(lexicon, 'whisper-prompt')`. `transcribeFile(wav, opts: TranscribeOptions { lexicon; whisperCli; model?; modelOptions: EnsureModelOptions; lang?; translate?; prompt?; exec?; tmpDir?; log? }): Promise<TranscribeResult { raw; output; normalized: NormalizeResult; model: ResolvedModel; prompt?; ms: { transcribe; normalize } }>`: ensures the model, runs whisper-cli (`MissingToolError` on `ENOENT`; a non-zero exit with no JSON throws with the stderr tail), reads and removes `<outBase>.json` from `tmpDir` (created if absent), cleans, normalizes.
- `src/voice/history.ts`: `HISTORY_MAX_LINES = 1000`, `historyPath(globalPath)` = `voice/history.jsonl`, `HistoryEntry { at; raw; output; model; ms: { record; transcribe; normalize } }`, `appendHistory(globalPath, entry, max?)` (atomic rewrite keeping the newest `max` lines; never throws), `readHistory(globalPath)` (malformed lines skipped).
- `src/voice/voice.ts`: `EXIT_OK = 0`, `EXIT_ERROR = 1`, `EXIT_MISSING_TOOL = 2`, `EXIT_NOTHING_HEARD = 3`. `VoiceOptions { cwd?; globalPath?; seconds?; device?; model?; lang?; translate?; prompt?; history?; copy?; paste?; json?; quiet? }`, `VoiceDeps { exec?; spawn?; platform?; env?; isAlive?; kill?; sleep?; download?; waitForStop?: () => Promise<'enter' | 'sigint'>; clipboardWrite?; sendPaste?; exists?; extraBinDirs?; modelFallbackDirs?; tmpDir?; now?; clock? }`, `VoiceIO { stdout; stderr }`, `VoiceJsonResult { raw; output; replacements; summary; model; seconds; ms: { record; transcribe; normalize } }`.
  - `runVoice(opts, io, deps?)`: locates both tools (exit 2 with `installHint` on stderr when either is missing), loads the merged lexicon, resolves the input, records to `<tmpDir>/lexicon-voice-<pid>-<t>.wav` (attached child; `--seconds` waits for ffmpeg to exit, otherwise `waitForStop` (default: Enter on stdin or SIGINT) then `stopRecorder`), fails with a Microphone-permission hint when no audio was written, transcribes, deletes the WAV and delivers. A whisper failure keeps the WAV and names it.
  - `runVoiceToggle(opts, io, deps?)`: state file present and pid alive -> stop: `stopRecorder`, clear the state file, transcribe the WAV, deliver, delete the WAV (`ffmpeg` is not required for the stop half). Otherwise -> start: a stale state file is removed (with its WAV) and logged, both tools are located, ffmpeg is spawned detached with `-t min(seconds, 600)` and stderr to `voice/recorder.log`, `{ pid, wav: voice/recording-<iso>.wav, startedAt }` is written, `recording` is printed, exit 0.
  - `deliver(result, recordMs, seconds, opts, io, deps, r)`: empty raw -> `(nothing heard)` (or the JSON result) and exit 3, nothing recorded; otherwise history append (unless `history: false`), `recordHits` for the changed canonicals (best-effort, awaited), a `N corrections: ...` status line on stderr, then the text (or `jsonResult(...)`) on stdout; `copy`/`paste` write the clipboard via the daemon backend (`detectClipboardBackend` over the voice exec) or `clipboardWrite`; `paste` on darwin runs `osascript -e PASTE_APPLESCRIPT` (or `sendPaste`) with an Accessibility hint on failure; off darwin it prints "not supported yet" and behaves as `copy`. Clipboard or paste failures exit 1 after the text was already printed.
  - `runVoiceStatus(opts, io, deps?)`: `recording since <startedAt>` / `idle` (`--json`: `{ recording, since?, pid? }`), exit 0 when recording else 1. `runVoiceListDevices(opts, io, deps?)`: `[N] Name` lines (or JSON), exit 2 without ffmpeg.
- `src/cli/cmd-voice.ts`: `registerVoiceCommands(program, io)` adds `voice` (`--toggle`, `--status`, `--list-devices`, `--seconds <n>`, `--device <name|index>`, `--model <name|path>` default `base.en`, `--lang <code>` default `en`, `--translate`, `--no-prompt`, `--no-history`, `--copy`, `--paste`, `--json`, `--quiet`) and dispatches to the four runners above; the exit code becomes `process.exitCode`.
- `runDoctor` (commands.ts) adds warnings, never failures: `whisper-cli found: <path>` / `whisper-cli not found (...)`, `ffmpeg found` / `not found`, an info line `whisper model base.en present: <path>` or `absent (... downloads it to <path> on first run)`, and on darwin an info line about Microphone permission for the terminal or launcher.

## src/cli/prompt.ts
Dependency-free interactive helper over `node:readline`. No ANSI beyond bold/dim, and only when the output is a TTY.
- `Prompter { ask(question, opts?: { default? }): Promise<string>; confirm(question, def?): Promise<boolean>; choose<T>(question, choices: PromptChoice<T>[], opts?: { multi? }): Promise<T[]>; close(): void }`, `PromptChoice<T> = { label; value }`.
  - `ask`: Enter returns `opts.default` (or `''`). `confirm`: Enter returns `def` (default false), accepts y/yes/n/no.
  - `choose` single: numbered list, a number (Enter = first). Multi: every item starts selected, numbers toggle, `a` all, `n` none, Enter accepts. Empty choices resolve to `[]`.
  - A prompt whose input closes (EOF) rejects with `PromptClosedError`.
- `createPrompter(io?: PrompterIO { input?; output? }): Prompter` (defaults to process stdin/stdout; readline is created lazily).
- `isInteractive(): boolean` (`process.stdin.isTTY && process.stdout.isTTY`).
- `askKey(prompter, question, keys: readonly string[], def?): Promise<string>` first character of the line, lower-cased, repeated until it is one of `keys`; empty line returns `def` when given.
- `splitList(input): string[]` comma-separated, trimmed, empties dropped, case-insensitive dedupe (first spelling wins).
- `styler(output?): Styler { bold(s); dim(s) }`, `PromptClosedError`.

## src/cli/commands.ts
- `IO { stdout; stderr }`, `processIO`, `CommonOptions { cwd? }`, `renderTable(rows, headers)`, `readStdin(maxBytes?)`, `whichBin(name)`.
- Handlers: `runInit(opts: InitOptions)`, `runAdd(canonical, aliases, opts: AddOptions, io, prompter?)`, `runRemove`, `runList`, `runNormalize` (`buildNormalizeOptions`; always exits 0 and passes input through on lexicon load errors), `runHarvest(root, opts: HarvestCliOptions, io, deps?: HarvestDeps { isInteractive?; createPrompter? })`, `runExport`, `runPath`, `runDoctor(opts, io, deps?: DoctorDeps)` = `renderDoctorReport(await runDoctorReport(opts, deps), io)`, `runDoctorReport(opts, deps?): Promise<DoctorReport>` (the checks as data, no output), `renderDoctorReport(report, io): number` (one line per check, blank line, verdict; returns the exit code), `runInstallClaude(opts: InstallClaudeOptions, io, deps?: InstallClaudeDeps)`.
- `DoctorLevel = 'ok'|'fail'|'warn'|'info'`, `DoctorCheck { level, message }` (message unsanitized; renderers apply `safeLines`), `DoctorReport { ok (no `fail`), checks: DoctorCheck[], paths: { global, project?, trust (getTrustPath), settings, installedPlugins }, versions: { lexicon (package.json two levels up, else '0.0.0'), node (process.version), platform } }`. `DoctorDeps { platform?; env?; exec?; settingsPath?; installedPluginsPath?; home?; uid? }`. `list`, `normalize` and `doctor` warn on stderr when a project lexicon was skipped.
- `checkLoginService({ platform, env, exec, home, uid? }): Promise<DoctorCheck>` (one check, appended by `runDoctorReport` before the clipboard check): darwin runs `launchctl print gui/<uid>/<serveLabel(env)>` (loaded when it does not throw) and reads `launchAgentPath(home, env)` for `programPathFromPlist`; linux runs `systemctl --user is-active lexicon-serve.service` (loaded when stdout is `active`) and reads `systemdUnitPath(home, env)` for `programPathFromUnit`. Neither loaded nor a file -> `info` `no login service installed (optional; keeps the local API up: lexicon serve --install)`; a program path that does not exist -> `fail` `login service <name> points at a missing file: <path> (run: lexicon serve --uninstall && lexicon serve --install)` (checked before the loaded state: a crash-looping service is the actionable problem); file present but not loaded -> `warn` `... is installed at <file> but not loaded (...)`; loaded -> `ok` `login service <name> loaded (<path> exists)`. Other platforms -> `info` `login service is not automated on <platform> (...)`.
- `runAdd` with `-i` (`AddOptions.interactive`): when no aliases were given the suggestions become a multi-select checklist followed by `more aliases (comma-separated, Enter for none)`; then `phonetic hint` (default `--phonetic`, empty = none) and `category` (default `--category` or `other`, re-asked until valid). Without a prompter and not on a TTY throws `add --interactive needs a terminal ...`. `AddOptions.globalPath` is a test hook.
- `runHarvest`: `--interactive`, or `--add` on a TTY without `--yes`/`--json`, hands the candidates to `runHarvestInteractive` (cmd-review.ts); otherwise prints the table and, with `--add`, adds every candidate. `--interactive` off a TTY throws `harvest --interactive needs a terminal ...`; `--interactive` + `--json` is rejected.
- `runDoctor`: three tiers, `✓` ok, `!` warn, `✗` fail (exit 1), `·` info. Checks both lexicon files, trust status, term conflicts, the Claude Code wiring and the clipboard backend. `DoctorDeps` gains `settingsPath?` (default `~/.claude/settings.json`) and `installedPluginsPath?` (default `~/.claude/plugins/installed_plugins.json`). `findInstalledLexiconPlugin(settings, installedPlugins): string | undefined` (a `lexicon@<marketplace>` key under `installed_plugins.json` `plugins`, or a true `enabledPlugins` entry) -> `✓ lexicon plugin installed as <id>`; otherwise per event `settingsHasLexiconHook(settings, event)` (a hook command matching `user-prompt-submit.js`, `plugin/hook.mjs` or `lexicon`) -> `✓` or the warning `! <event> hook not found in <settings> (fine if you use the plugin; otherwise run: lexicon install-claude --apply)`. An unparseable settings.json is a warning. When the plugin is installed, a missing `claude mcp list` entry is a warning instead of a failure. A missing clipboard backend is a warning.
- Claude Code integration helpers: `HOOK_EVENTS: readonly string[] = ['UserPromptSubmit', 'SessionStart']`; `resolveIntegrationPaths(cliDir?): IntegrationPaths { server; hook; bundled }` returns `<root>/plugin/mcp-server.mjs` + `<root>/plugin/hook.mjs` when both exist (root = `cliDir/../..`; without `cliDir`, `cliDir` = `<findPackageRoot(import.meta.url)>/dist/cli`, falling back to this module's directory), else `dist/mcp/server.js` + `dist/hooks/user-prompt-submit.js`; `InstallOutcome = 'created' | 'updated' | 'unchanged'`; `InstallClaudeDeps { cliDir?; settingsPath?; exec?; onWritten?: (file, outcome) => void }` (`onWritten` reports settings.json under `--apply`); `hookConfigFor(command, timeout = 5, events = ['UserPromptSubmit'])`, `mergeHookIntoSettings(settings, command, timeout = 5, events = ['UserPromptSubmit'])` (one group per event; an event that already has the identical command is skipped; `changed` is true when any event was added); `CLAUDE_MD_SNIPPET`. `runInstallClaude` prints the three steps and, with `--apply`, runs `claude mcp add --scope <user|project> lexicon -- node "<server>"` and merges hooks for every `HOOK_EVENTS` entry (paths quoted).

## src/cli/cmd-import.ts
- `MAX_IMPORT_BYTES` (8 MB; files and stdin over it are refused before parsing).
- `registerImportCommands(program, io)` adds `import <file> [format]` (`-` reads stdin) with `--format <f>` (default auto), `--project`, `--dry-run`, `--source <s>`, `--category <c>` (applied to terms lacking one), `--json`.
- `runImport(file, opts: ImportCliOptions, io, readInput?): Promise<number>` the testable handler. Prints a canonical/aliases/status table and `imported N terms (M new, K merged, S skipped)`; skipped reasons go to stderr; unknown format lists `IMPORT_FORMAT_INFO` and returns 1. `--dry-run` reads the target file to label new vs merged without writing. Project-scope writes go through `addTerm` and therefore the trust gate.

## src/cli/cmd-install.ts
`lexicon install [client]` (`install-claude` stays in index.ts). Registered via `registerInstallCommands(program, io)`.
- `INSTALL_CLIENTS: readonly ['claude','codex','cursor','windsurf','gemini','claude-desktop','vscode','generic']`, `InstallClient` union.
- `runInstall(client: string | undefined, opts: InstallOptions, io, deps?: InstallDeps): Promise<number>`
  - `InstallOptions extends CommonOptions { apply?; scope?: 'user'|'project' (string, validated); project?; home? }`; `InstallDeps { platform?; env?; cliDir? }` (test hooks, like `DoctorDeps`).
  - `claude` delegates to `runInstallClaude` (scope passed through; `--home` maps to `settingsPath`). `generic` / no client prints the `mcpServers` snippet and the client list. Unknown client throws.
  - Server path from `resolveServerPath(cliDir?)`, which delegates to `resolveIntegrationPaths` (bundle preferred, `dist/mcp/server.js` fallback). `InstallDeps { platform?; env?; cliDir?; onWritten?: (file, outcome: InstallOutcome) => void }`; `onWritten` is called once per config file under `--apply` (forwarded to `runInstallClaude` for `claude`).
  - Without `--apply` prints the file and the exact block/JSON that would be merged. With `--apply` reads the file (missing => `{}` / empty), merges, writes 2-space JSON + trailing newline (or TOML text), reports `created` / `updated` / `nothing changed`. Never clobbers unrelated keys or other servers; a stale `lexicon` entry is replaced; invalid JSON or a non-object server map throws without writing.
  - Always ends with the hint `lexicon export claude-md >> <rules file>` (CLAUDE.md, AGENTS.md, .cursor/rules/lexicon.mdc, .windsurfrules, GEMINI.md, copilot-instructions.md).
- `configPathFor(client, ctx: { home, cwd, platform, env, project }): string`: codex `~/.codex/config.toml` | `./.codex/config.toml`; cursor `~/.cursor/mcp.json` | `./.cursor/mcp.json`; windsurf `~/.codeium/windsurf/mcp_config.json`; gemini `~/.gemini/settings.json` | `./.gemini/settings.json`; claude-desktop macOS `~/Library/Application Support/Claude/claude_desktop_config.json`, win32 `%APPDATA%/Claude/...`, else `~/.config/Claude/...`; vscode macOS `~/Library/Application Support/Code/User/mcp.json`, win32 `%APPDATA%/Code/User/mcp.json`, linux `~/.config/Code/User/mcp.json`, project `./.vscode/mcp.json`. windsurf and claude-desktop throw on `--project`. `targetFor(client, ctx)` bundles path, key and entry.
- `mergeServerIntoJson(current: unknown, key: 'mcpServers'|'servers', entry): MergeResult { next, changed }` pure, never mutates input. vscode uses `servers` + `{ type: 'stdio', command, args }` (`VsCodeServerEntry`); everyone else `mcpServers` + `{ command: 'node', args: [serverPath] }` (`StdioServerEntry`).
- `codexBlock(entry): string` => `[mcp_servers.lexicon]\ncommand = "node"\nargs = ["<abs server path>"]` (`CODEX_TABLE`; strings escaped with JSON.stringify, valid TOML basic strings). `upsertTomlTable(text, header, block): { next, changed }` replaces the table from its header line up to the next `[` header (sub-tables such as `[mcp_servers.lexicon.env]` survive), else appends after a blank line. No TOML library.

## src/cli/cmd-trust.ts
- `registerTrustCommands(program, io)` adds `trust [path]` (`--list`) and `untrust [path]`.
- `runTrust(target, opts: TrustOptions { list? }, io)`: resolves the project file for cwd (or the given path), refuses a missing or invalid file, prints a preview table (canonical, first alias, capped rows), then `trustProject` and reports `trusted` / `updated` / `re-pinned` with the registry path. `--list` prints every registry entry with its status (`trusted`, `changed`, `missing`) and notes when `LEXICON_TRUST_ALL` is set.
- `runUntrust(target, opts, io)`: `untrustProject`; reports when the file was not trusted.

## src/cli/cmd-learn.ts
- `registerLearnCommands(program, io)` adds `learn [words...]` (`--from <sentence>`, `--project`, `--json`) and `stats` (`--json`).
- `correctionFromArgs(words, from): Correction | undefined`: `--from` is parsed with `parseCorrection`; exactly two words are `<heard> <meant>`; any other word list is joined and parsed as a sentence.
- `runLearn(words, opts: LearnOptions, io)`: prints the usage examples (`CORRECTION_EXAMPLES`) and returns 1 when nothing parses; otherwise `learnCorrection` and reports the alias added (or already known) and the file. `--project` forces `scope: 'project'`.
- `runStats(opts: StatsOptions, io)`: `computeStats(await loadLexicon({ cwd }))`, rendered by `renderStats(stats)` or as JSON.

## src/cli/cmd-review.ts
Interactive workflows. Every handler takes a `Prompter` (or an injectable spawner) so tests script the answers. `InteractiveOptions extends CommonOptions { globalPath?: string }` is a test hook (no CLI flag) forwarded to the store.
- `runHarvestInteractive(candidates: readonly HarvestCandidate[], opts: HarvestInteractiveOptions, io, prompter: Prompter): Promise<number>`: `opts.cwd` is the harvested root (the project lexicon written to). Checks the trust gate up front: an existing untrusted/changed `.lexicon.yaml` prints the `ProjectTrustError` message to stderr and returns 1 before any prompt; a `ProjectTrustError` thrown mid-walk does the same. Per candidate shows `[i/n] canonical  category, seen Nx`, up to three evidence entries and the suggested aliases, then asks `add? [y/n/e/c/a/q]` (Enter = y): `y` `addTerm(scope: 'project')`, `n` skip, `e` replace aliases with a comma-separated list (the canonical itself is dropped), `c` change category (re-asked until valid), `a` add this and every remaining candidate without asking, `q` stop (the rest counts as skipped). Candidates are copied before editing. Ends with `added N new terms, merged M, skipped S in <path>` and the trusted note.
- `runReview(opts: ReviewOptions, io, prompter?): Promise<number>`: `ReviewOptions extends InteractiveOptions { neverHit?; project?; global?; category? }`. Global file by default, `--project` the project file found for cwd (error when none; `--project` + `--global` is an error). Without a prompter and not on a TTY throws `review needs a terminal ...`. A missing file or an empty selection prints a note and returns 0. An untrusted project file returns 1 with the `ProjectTrustError` message. Per term shows `[i/n] canonical  category, N hits[, source]`, aliases, phonetic and notes, then `[k/d/e/p/n/q]` (Enter = k): `k` keep, `d` delete, `e` edit aliases (comma-separated), `p` phonetic hint (empty clears), `n` notes (empty clears), `q` stop. Writes via `writeLexiconFile` once at the end only when something changed, then `refreshTrust` for a project file. Ends with `kept K, deleted D, edited E, wrote <path>` or `(nothing written)`.
- `runEdit(opts: EditOptions, io, spawnEditor?: SpawnEditor, env?): Promise<number>`: `EditOptions extends InteractiveOptions { project? }`; `SpawnEditor = (command, args, filePath) => Promise<number>` (exit code), default `spawnEditorInherit` (`spawn` with `stdio: 'inherit'`). A missing global file is created empty first. Editor from `$VISUAL` then `$EDITOR`, split on whitespace (`editorCommand(env)`); none set prints the path and returns 0. After the editor exits (a non-zero code is reported on stderr and the file still checked) the file is re-read with `readLexiconFile` (which uses `parseLexicon`): on error prints the message plus `your edits are still in <path>` and returns 1 without touching the file. A project file that was trusted before the edit is re-pinned (`refreshTrust`); an untrusted one stays untrusted with a `lexicon trust` hint.
- `registerReviewCommands(program, io)` adds `review` (`--never-hit`, `--project`, `--global`, `--category <c>`) and `edit` (`--project`).

## src/cli/cmd-suggest.ts
- `registerSuggestCommands(program, io)` adds `suggest` (`--json`, `--apply`, `--yes`, `--harvest [dir]`, `--limit <n>`, `--project`). The global `--cwd` is accepted after the subcommand too; giving it explicitly (or `--harvest`) turns the repo harvest on, a bare `lexicon suggest` only reads the history.
- `runSuggest(opts: SuggestCliOptions, io, deps?: SuggestDeps): Promise<number>`: `SuggestCliOptions extends CommonOptions { json?; apply?; yes?; harvest?: string | boolean; limit?; project?; globalPath? (test hook) }`; `SuggestDeps { suggest?; loadLexicon?; addTerm?; removeTerm?; prompter?; now? }` default to the core. Loads the lexicon (an untrusted project file is reported on stderr as elsewhere), calls `suggestTerms({ loaded, cwd?, limit?, now? })`, then: `--json` prints the array and applies nothing; no suggestions prints a note and returns 0; the default prints `renderSuggestions(suggestions)` (a `renderTable` with kind, canonical, alias, count, confidence, reason) plus a footer; `--yes` applies every suggestion with `confidence >= AUTO_APPLY_CONFIDENCE` without asking and reports `applied N suggestions, skipped M below 0.80`; `--apply` walks them with a Prompter (`deps.prompter`, else a TTY is required and the error suggests `--yes`), showing kind, confidence, count, reason and up to three evidence lines, then `apply? [y/n/a/q]` (Enter = y, except a 'stale' removal defaults to n and is never swept up by `a`). `--apply` with `--yes` is an error (exit 1).
- Applying: 'alias' -> `addTerm({ canonical, aliases: [alias] }, { scope })` (merge); 'never' -> `addTerm({ canonical, aliases: [], never: [alias] }, { scope })`; 'term' -> `addTerm({ canonical, aliases: suggestion.aliases ?? suggestAliases(canonical), category?, source: 'learned' }, { scope: --project ? 'project' : 'global' })`; 'stale' -> `removeTerm(canonical, { scope })`. For existing terms `scope` is the term's own (`term.scope`, default global). A `ProjectTrustError` prints the message and returns 1; other store errors propagate.

## src/cli/cmd-setup.ts
`lexicon setup`, the guided first run. Registered via `registerSetupCommands(program, io)`: `setup` with `-y/--yes`, `--clients <list>`, `--company <name>`, `--person <name>`, `--phonetic <hint>`, `--app <wispr|superwhisper|macos|none>`, `--export-dir <dir>`, `--harvest` / `--no-harvest`, `--serve` / `--no-serve`, `--reseed`, `--home <dir>`, `--json`, `--dry-run`.
- `SETUP_CLIENTS` (= `INSTALL_CLIENTS` without `generic`), `SetupClient`; `SETUP_APPS = ['wispr','superwhisper','macos','none']`, `SetupApp`; `HARVEST_LIMIT = 10`, `HARVEST_MIN_COUNT = 5`, `SEEDED_TERMS = 3`.
- `SetupOptions extends CommonOptions { yes?; json?; reseed?; clients?: string; company?; person?; phonetic?; harvest?; serve?; app?: string; exportDir?; home?; dryRun? }`. `serve` and `harvest` are three-valued: `true` does it, `false` skips it without a prompt, `undefined` asks on a terminal (default yes) and is treated as `false` without one (`--yes`, no TTY, `--json`), so a non-interactive run never creates a service, or writes and trusts a project lexicon, that was not asked for. A dry run still lists the harvest candidates in `wouldHarvest` unless `harvest === false`.
- `SetupPlan { plan: true; lexiconPath; lexiconExists; wouldSeed: string[]; wouldHarvest: string[]; detectedClients: string[]; wouldInstallClients: string[]; wouldInstallServe: boolean; wouldExport: { format; path }[] }`: what a run would do, computed by `dryRun` without writing.
- `SetupSummary { lexiconPath; termsAdded: string[]; clients: SetupClientResult { name; status: 'installed' | 'skipped' | 'failed'; detail? }[]; serve: 'installed' | 'skipped' | 'failed'; exports: { format; path }[] }`. `termsAdded` lists only terms created (merges into existing terms are not counted), global seed first, then harvested project terms. `detail` for `installed` is the tildified config file(s) the installer reported through `onWritten`, comma separated, `(unchanged)` appended to one that already had the entry (the installer's last output line when nothing was reported); for `failed` the last error line.
- `SetupDeps { platform?; env?; home?; exists?: (p) => boolean; exec?: SetupExec ((file, args) => string, trimmed stdout, throws on failure; default execFileSync with a 5 s timeout); cliDir?; isInteractive?; createPrompter?; installClient?: (client, opts: InstallOptions, io, onWritten: (file, outcome: InstallOutcome) => void) => Promise<number> (default runInstall with `{ platform, env, onWritten, cliDir? }`); installServe?: (opts: ServeOptions, io, deps: ServeDeps) => Promise<number> (default runServeInstall; `deps` = `{ platform, env, home? (when overridden), cliPath? = <cliDir>/index.js when cliDir is set }`); harvest?: (root, opts) => Promise<HarvestCandidate[]> (default harvestRepo) }`. Every process spawn and filesystem probe goes through these.
- `runSetup(opts: SetupOptions, io: IO, deps?: SetupDeps): Promise<{ code: number; summary: SetupSummary; plan?: SetupPlan }>`. Interactive (prompt.ts) when none of `yes`, `json`, `dryRun` is set and `isInteractive()`; otherwise every default is taken (off a TTY without `--yes` it says so). Under `dryRun` every step records what it would do in `plan` instead of doing it, nothing prompts, and the summary card becomes a plan card ending in `run again without --dry-run to apply`. Under `--json` progress goes to stderr and stdout carries only the summary JSON. `code` is 1 when any client or the serve install failed; every step still runs. Steps, each one line of output:
  1. Global lexicon: `runInit` when missing. Seed unless the file already has `SEEDED_TERMS` terms (or `--reseed`): the person term from `--person` or `git config --global user.name` (confirmed on a terminal; category `person`, `suggestAliases`), then the company term from `--company`, else the prompt with a default from `suggestCompany(packageName, remoteUrl)` (cwd `package.json` scope `@ashlr/x` -> `Ashlr`, else the git remote org `github.com/ashlrai/x` -> `ashlrai`; `users`/`orgs` path segments are rejected), category `brand`, `suggestAliases`, `--phonetic` or the prompt. Non-interactive with neither `--company` nor a suggestion: no company term. On a terminal `add another term?` loops (name, phonetic, category). Writes go through `addTerm(scope: 'global')`, so reruns merge instead of duplicating; a person or company whose canonical is already in the global lexicon (case-insensitive) is reported as `already present: <existing spelling>` and not written or counted (the plan leaves it out of `wouldSeed` too).
  2. Repo harvest (skip with `--no-harvest`; without a terminal only with `--harvest`, else `skipped (not requested)`; a dry run always lists): when cwd is inside a git repo, `harvest(root, { limit: 10, minCount: 5 })`, candidates whose canonical the global lexicon or this run's seed already covers are dropped (`already in the global lexicon: ...`), a table plus a confirm on a terminal, then `addTerm(scope: 'project', cwd: root)` for every candidate (auto-trusted by the store); a `ProjectTrustError` is reported as skipped.
  3. Agent clients: `--clients a,b` (validated by `parseClientList`; `none` skips) or `detectClients(deps, cwd): DetectedClient { name; detected; evidence? }[]` (config dir from `configPathFor` / `~/.claude`, the app bundle under `/Applications` on darwin for Claude, Cursor, VS Code and Windsurf, or the CLI on PATH via `findOnPath` with the injected `exists`; never spawns). On a terminal the detected clients are a multi-select with all pre-selected. Each chosen client runs `installClient(client, { apply: true, cwd, home? }, capturedIO, onWritten)`; the files reported through `onWritten` become `detail` (the last output line when none was reported), a non-zero code or a throw is `failed` with a retry hint on stderr.
  4. Local API (skip with `--no-serve`; without a terminal only with `--serve`): on darwin/linux (confirm on a terminal, default yes) `installServe({ cwd }, capturedIO, serveDeps)` with `serveDeps.cliPath = <cliDir>/index.js` when `cliDir` is set -> `serve: 'installed' | 'failed'` (the installer's refusal of a missing program path is a `failed` with its stderr line); other platforms are `skipped` with a pointer to `lexicon serve --install`.
  5. Dictation app: `--app` or a single choice (Wispr Flow, Superwhisper, macOS Text Replacement, none; non-interactive default none). Exports the merged lexicon to `<--export-dir | ~/Desktop | dirname(global)>/lexicon-<format>.<ext>` and prints where to import it.
  6. Summary card: lexicon path, terms added, clients, local API, exports, and three next actions (dictate a sentence with the company name in Claude Code, `lexicon suggest` after a week, `lexicon voice --list-devices`).

## src/cli/index.ts  (bin: lexicon)
commander wiring only; handlers live in `commands.ts` and `cmd-*.ts`. Global `--cwd <dir>`. Commands defined here: `init` (`--project`), `add <canonical> [aliases...]` (`--phonetic`, `--category`, `--notes`, `--project`, `--suggest`, `-i/--interactive`, `--never <word...>`), `remove|rm <canonical>` (`--project`), `list|ls` (`--json`, `--category`, `--query`), `normalize [text...]` (stdin if omitted; `--json`, `--diff`, `--dry-run`, `--min-confidence`, `--no-phonetic`, `--no-fuzzy`, `--include-untrusted`), `harvest [path]` (`--limit`, `--min-count`, `--add`, `-i/--interactive`, `--yes`, `--json`), `export [format]` (`--out`, `--category <c...>`, `--limit`), `path`, `doctor`, `mcp`, `hook`, `daemon` (`--once`, `--paste`, `--interval`, `--dry-run`, `--quiet`, `--backend`, `--which`), `install-claude` (`--apply`, `--scope`). Then `registerImportCommands`, `registerInstallCommands`, `registerTrustCommands`, `registerLearnCommands`, `registerReviewCommands`, `registerSetupCommands` add `import`, `install`, `trust`, `untrust`, `learn`, `stats`, `review`, `edit`, `setup`. `docs/CLI.md` is generated from their `--help`.

## src/hooks/user-prompt-submit.ts  (bin via `lexicon hook`; bundled as plugin/hook.mjs)
Claude Code hook entry for two events, dispatched on the payload's `hook_event_name`. Exit 0 always; every error path prints nothing (message to stderr). Must finish < 200ms for a 1KB prompt.
- `HookEventName = 'UserPromptSubmit' | 'SessionStart'`, `HookOptions { cwd? }`, `SESSION_CONTEXT_MAX_CHARS = 4000`.
- `runHook(input: string, opts?: HookOptions): Promise<string>` is the dispatcher `main()` uses: `SessionStart` -> `runSessionStartHook`; anything else (including a missing event name) -> `runUserPromptSubmitHook`. Returns the JSON to print or `''` (print nothing).
- `runUserPromptSubmitHook(input, opts?)` reads `{ prompt, cwd, ... }`, runs normalize, and prints `{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"<parts joined by \n>"}}` when any part exists. Parts, in order: the corrections note (`formatAdditionalContext(result)`: diff summary + corrected prompt) when normalize changed something; the correction note (`formatCorrectionNote(correction)`) when `parseCorrection(prompt)` yields both sides non-empty: `The user is correcting a spelling: "<heard>" should be "<meant>". Call the lexicon learn_correction tool with heard: "<heard>", meant: "<meant>". If "<heard>" contains words that are not part of the misspelled name, pass only the name. Then continue with the rest of the message.` (the hook never writes to the lexicon). When a correction is detected the normalize result first passes through `dropCorrectionSpans(result, correction): NormalizeResult` (exported, pure): every replacement whose span overlaps a case-insensitive occurrence of `heard` or `meant` in the prompt, or whose original text equals one of them, is dropped, `output` and `changed` are rebuilt from the rest, and hits are recorded only for the remaining canonicals; the same object is returned when nothing overlaps; after the correction pass the result goes through `dropDataBlockSpans(result)` (exported, pure): every replacement inside a span from `dataBlockSpans(input)` is dropped the same way and records no hit. `dataBlockSpans(text): [start, end)[]` (exported) marks lines that read as pasted dictionary or CSV data: a header found by `DATA_HEADER_RE` (`word,` / `canonical,` / `original,` / `shortcut,` followed by a non-space, at the line start or after whitespace, `:`, `;` or `(`; the span starts at the token so a prose prefix such as `import this: word,replacement` stays correctable) together with the following consecutive rows with the same comma count, or any run of at least `DATA_BLOCK_MIN_ROWS` (3) consecutive rows with the same comma count; a row is a line with one or more commas, none with whitespace on either side and none at either end (`versel,Vercel`, `a,b,brand`), so prose commas (`ping X, then`) never count. The skipped-project note (`formatSkippedProjectNote(loaded)`, one line naming the path, never the contents, telling the model to review through `trust_project` `status` or `lexicon trust` and not to open the file with Read or cat, and to trust only after the user says yes to the preview) when `loadLexicon` reports `skippedProject`.
- `runSessionStartHook(input, opts?)` when the merged lexicon has >= 1 term prints `{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"<claude-md export>[\n<skipped-project note>]"}}`. With 0 terms it prints the onboarding note `ONBOARD_NOTE` (one line: the lexicon is empty; offer setup, do not run it unasked; ask for (1) company/product names spelled exactly and their pronunciation, (2) the user's own name, (3) which of Claude Code, Claude Desktop, Codex, Cursor, Windsurf, Gemini CLI, VS Code they use; then call `setup_lexicon` with company, person and clients, or the onboard prompt; followed by the skipped-project note when there is one) at most once per `ONBOARD_NOTE_INTERVAL_MS` (24 h), else `''`. The gate is `shouldEmitOnboardNote(globalPath, now?)`: reads `onboardNotePath(globalPath)` = `<dirname(globalPath)>/onboard-note.json` (`{ lastNotedAt: ISO }`), returns false inside the interval, otherwise writes the new timestamp (best effort, mkdir -p, errors to stderr) and returns true; a missing or corrupt state file counts as never noted. The note is never emitted when terms exist. The export is capped by `truncateSessionContext(text, max = SESSION_CONTEXT_MAX_CHARS)`: table rows are dropped from the end and a line `... N more terms; read the lexicon://me resource for the full list.` is inserted before the footer (text without a table is hard-cut with `...`). Untrusted content never appears: the skipped file contributes its path only.
- `hooks/hooks.json` registers `plugin/hook.mjs` for both events; `lexicon install-claude --apply` merges both into `~/.claude/settings.json`.

## src/mcp/server.ts  (bin: lexicon-mcp; bundled as plugin/mcp-server.mjs)
- `createServer(opts?: ServerOptions { cwd? }): McpServer`, `main()`. Server name `lexicon`, stdio transport, version from `readPackageVersion()` (`findPackage()` looks for `package.json` two levels up for dist/src, then one level up for plugin/, accepting only `@ashlr/lexicon`; the same lookup gives `cliDirForInstall()` = `<package root>/dist/cli`, which the install helpers derive the package root from). Instantiated once; reads the lexicon fresh on every call; logs to stderr only. Sends `SERVER_INSTRUCTIONS` at initialize (read lexicon://me first, normalize dictated input, learn corrections, suggest before guessing, never rewrite code, offer setup_lexicon when the lexicon is empty after asking for the company spelling, the user's name and the clients, preview then apply with `clients`, `harvest: true` and `serve: true` only for what was agreed, lexicon_doctor when corrections stop, suggest_terms + apply_suggestion to improve, preview install_client before applying, never trust a project file unseen). The `trust_project` description adds that it replaces reading `.lexicon.yaml` (sanitized preview, notes never quoted, do not open the file with Read or cat).
- The agent-native tools import their CLI handlers statically (`runDoctorReport` from commands.ts, `runInstall` from cmd-install.ts, `runImport` from cmd-import.ts, `runSetup` from cmd-setup.ts, `suggestTerms`/`loadVoiceHistory` from the core). Never dynamic-import a module that is also in the static graph: esbuild then lazy-wraps the shared subgraph (zod included) and `plugin/mcp-server.mjs` fails at load.
- Every tool body runs inside `guarded()`, which turns a thrown error into an `isError` result. Tools:
  - `normalize_transcript({ text, dryRun?, minConfidence? })` -> `{ output, changed, replacements[], summary }`; on a real change calls `recordHits` for the matched canonicals (best effort).
  - `add_term({ canonical, aliases?, phonetic?, category?, notes?, never?, scope? })` -> `{ term, path, created }`; if aliases omitted, uses `suggestAliases`.
  - `remove_term({ canonical, scope? })` -> `{ canonical, removed }`.
  - `list_terms({ query?, category? })` -> `{ terms, counts: { matched, total, global, project }, paths, projectTrust?, skippedProject?, note? }` (skipped file reported by path only).
  - `harvest_repo({ path?, limit?, minCount?, add? })` -> `{ root, candidates, count, added?, failures? }`; with `add: true` writes to the harvested repo's own project lexicon (`cwd: root`); a `ProjectTrustError` is surfaced once as the tool error instead of per candidate.
  - `export_lexicon({ format, categories?, limit? })` -> the export as text.
  - `learn_correction({ heard, meant, scope? })` -> `{ term, path, created, aliasAdded, summary }`.
  - `suggest_canonical({ heard })` -> `{ heard, suggestions: { canonical, confidence, aliases, category? }[] }`.
  - `lexicon_stats({})` -> `LexiconStats`.
  - Agent-native (see `docs/AGENT-NATIVE.md`):
  - `lexicon_doctor({})` -> `DoctorReport` from `runDoctorReport({ cwd })` (src/cli/commands.ts).
  - `install_client({ client: 'claude'|'codex'|'cursor'|'windsurf'|'gemini'|'vscode'|'claude-desktop', apply?, scope?: 'user'|'project' })` -> `{ client, scope, applied, ok, output, stderr?, next? }`; runs `runInstall` (cmd-install.ts) with a buffered `IO` and `cliDir` from `cliDirForInstall()`; `apply` omitted or false is a preview that writes nothing (`next` tells the model to confirm with the user first).
  - `trust_project({ action: 'status'|'trust'|'untrust', path? })`; `path` resolves against `cwd`, default `resolvePaths({ cwd }).project`. `status` -> `{ action, registry, trustAll, trusted: TrustListEntry[], path?, status: TrustStatus|'none'|'missing'|'invalid', termCount?, preview?, more?, error? }`; `trust` parses first (invalid -> error `refusing to trust an invalid lexicon: ...`, missing -> error), then `trustProject` -> `{ action, path, previousStatus, status: 'trusted', result: 'trusted'|'updated'|'re-pinned', sha256 (12 chars), trustedAt, registry, termCount, preview, more, note }`; `untrust` -> `{ action, path, removed, registry, summary }`. `preview` is `trustPreview(file)`: the first 25 terms as `{ canonical, firstAlias?, aliasCount, hasNotes }` with every string through `sanitizeForDisplay`; notes are never quoted.
  - `import_dictionary({ path?, content? (max MAX_IMPORT_BYTES), format?: ImportFormat (default 'auto'), scope?, dryRun? })` -> the `ImportReport` JSON `runImport` (cmd-import.ts) prints with `json: true` (`content` is fed through the injected reader with `file: path ?? '-'`, so a `path` given alongside `content` only serves format detection); throws `pass path or content` when neither is given and relays the CLI's stderr when it exits non-zero.
  - `suggest_terms({ cwd?, limit? })` -> `TermSuggestion[]` from `suggestTerms({ loaded, history: await loadVoiceHistory(loaded.global.path), cwd, limit? })`; a failing `loadVoiceHistory` is logged and treated as empty.
  - `apply_suggestion({ suggestion: { kind, canonical, alias?, aliases?, category?, reason?, confidence?, evidence?, count? }, scope? })`: `alias` -> `addTerm({ canonical, aliases: [alias], source: 'user' })` (merge); `term` -> `addTerm` with the suggestion's `aliases` (trimmed, empties dropped), else `[alias]`, else `suggestAliases(canonical)`, plus its `category`; `never` -> `addTerm({ canonical, aliases: [], never: [alias] })`; `stale` -> `removeTerm(canonical)`. `alias`/`never` without an alias is an error. Returns `{ kind, canonical, term?, path?, created?, removed?, summary }`.
  - `setup_lexicon({ company?, person?, clients?: InstallClient[], harvest?, serve?, apply? })`: preview by default. Without `apply` it calls `runSetup({ cwd, yes: true, json: true, dryRun: true, company?, person?, clients: <comma-joined, or 'none' when omitted or empty>, serve: serve === true, harvest: false only when harvest === false })` and returns the `SetupPlan` (with `wouldHarvest` listed whether or not `harvest` was passed) plus a `next` line telling the model to show it and call again with `apply: true`; nothing is written. With `apply: true` it runs the same options without `dryRun` and with `harvest: harvest === true`, and returns `{ ok: code === 0, applied: true, summary: SetupSummary, stderr? }`; omitted `clients` install nothing (the plan's `detectedClients` is what the model offers), the repo is harvested only with `harvest: true` and the login service is installed only with `serve: true`. The description states what it asks for (company/product spelling, the user's own name, the clients) and what it does not do. A non-zero setup exit is reported as `ok: false`, not as an `isError`.
  - `serve_status({})` -> `GET http://127.0.0.1:41733/health` (1.5 s timeout): `{ up: true, url, ...health }` or `{ up: false, url, error, hint }` (never an `isError`).
- Resources: `lexicon://me` (text/markdown; the claude-md export, plus one line naming a skipped untrusted or changed project file), `lexicon://json` (application/json; the merged lexicon).
- Prompts: `voice-context` (the claude-md export followed by `VOICE_CONTEXT_INSTRUCTION`); `onboard` (no args; a user-role message, `ONBOARD_PROMPT`, under 25 lines: ask for company/product spellings and pronunciations, teammates, the clients in use, then call `setup_lexicon`, `add_term` for each remaining name showing the aliases, and end with one test sentence to dictate).

## plugin/ (committed esbuild bundles)
- `plugin/mcp-server.mjs` (<- `src/mcp/server.ts`) and `plugin/hook.mjs` (<- `src/hooks/user-prompt-submit.ts`): ESM, Node >= 20, every dependency inlined, unminified, shebang preserved, prefixed with a `createRequire` banner so bundled CommonJS (`yaml`) can `require` builtins. Built by `scripts/build-bundle.mjs` (`npm run build:bundle`); `npm run check:bundle` fails when they differ from `src/`. `.mcp.json` and `hooks/hooks.json` point at them via `${CLAUDE_PLUGIN_ROOT}` (`.mcp.json` uses `${CLAUDE_PLUGIN_ROOT:-.}` so it also works when the repo itself is the cwd); `dist/` stays untracked.

## extension/ (browser extension)
Manifest V3 extension for the ChatGPT, Claude, Grok, Gemini, Perplexity, Poe and Copilot composers. Built by `scripts/build-extension.mjs` (`npm run build:extension`) into `extension/dist` (Chromium) and `extension/dist-firefox`; the build fails if a `node:` builtin reaches a bundle or a non-core bundle exceeds 200 KB. The full contract, including the adapter table, modes, popup and privacy rules, is `docs/EXTENSION.md`.
- `extension/src/core.ts`: the browser-safe slice of `src/core` (schema, matcher, normalize, suggest) plus `yaml`, re-exported as one ESM chunk shared by the worker and the options page. It must never import store, trust, harvest or anything that touches `node:` modules.
- `extension/src/adapters.ts`: `SiteAdapter { host; composer selectors; send button selectors }`, `ADAPTERS`, `GENERIC`, `adapterFor(hostname)`, `hostKey(hostname)`, `findComposer(doc, adapter)`, `findSendButton(doc, adapter, composer)`. The "Supported sites" table in `docs/EXTENSION.md` mirrors `ADAPTERS`.
- `extension/src/background-core.ts`: `createBackground(deps: BackgroundDeps): Background` holds the token and settings (`chrome.storage.local` via `chrome-store.ts` `KeyValueStore`), calls `GET /health` (cached `HEALTH_TTL_MS`), `POST /normalize`, `POST /learn` and `GET /lexicon` on `DEFAULT_BASE_URL` (`http://127.0.0.1:41733`) with `FETCH_TIMEOUT_MS`, and falls back to `embedded.ts` `createEmbedded()` (`compileYaml`, `learnIntoYaml`, `lexiconToYaml`) when the API is down. Message types: `Request`, `Reply` (`NormalizeReply`, `LearnReply`, `StatusReply`, `SyncReply`, `PairReply`). `{ type: 'pair', baseUrl, token }` (from `pair.ts`): `baseUrl` must pass `pairableBaseUrl` (plain `http:`, host in `PAIR_HOSTS` = `127.0.0.1` | `localhost`, explicit port, no path/query/credentials) and `token` must match `TOKEN_RE` (32..128 hex); the token is proven with `GET /stats` under that bearer (`/health` needs no auth so it proves nothing), only then `saveSettings({ baseUrl, token, mode: 'api' })`, then `/health` for the reply `PairReply { ok: true, baseUrl, terms, version }`; any failure is `Failure` and stores nothing. `background.ts` is the `chrome.runtime` wiring.
- `extension/src/pair.ts` (built as the classic `pair.js`, manifest `content_scripts` matching `http://127.0.0.1:41733/pair*` and `http://localhost:41733/pair*` at `document_idle`; both origins are `host_permissions`): `pairFromDocument({ doc, send }): Promise<PairOutcome { ok, status }>` hides `#install`, reads `PAIR_META` (`lexicon-token`, `lexicon-port`; base URL = `http://<page hostname>:<meta port>`), sends `{ type: 'pair' }`, and writes `Paired with lexicon serve (N terms). You can close this tab.` (class `ok`, `<h1>` becomes `Paired`) or `Pairing failed: <error>` (class `error`) into `#status`; a page without a token is refused without a message. Auto-runs only when `chrome.runtime.id` is set (never under the tests). `pairUrl(baseUrl)` in `shared.ts` builds the `/pair` link the Options page shows ("pair in this browser" / "pair again") next to a "Paired with <baseUrl> (token stored)" / "Not paired" line; Options listens to `chrome.storage.onChanged` so a pairing in another tab is reflected without a reload.
- `extension/src/content-core.ts`: `installContent(deps: ContentDeps): ContentHandle` intercepts Enter and send-button clicks in the capture phase, sends the composer text to the worker, applies `applyCorrections` / `invertCorrections` through `editable.ts` (`readText`, `setWholeText`, `caretOffset`, `isEditable`, `editableRoot`; ranged `insertText` for ProseMirror, Lexical and Quill, the React-safe setter for textareas), shows `toast.ts` `showToast` with Undo for `TOAST_MS`, then re-dispatches the original event. Live mode debounces `LIVE_DEBOUNCE_MS`; the unchanged-text precheck `PRECHECK_DEBOUNCE_MS`. The token never reaches this script. `content.ts` is the `chrome.*` wiring.
- `extension/src/popup.ts`, `options.ts`, `shared.ts`: popup (status, per-site switch, last `RECENT_LIMIT` corrections, Learn a word) and options (token, mode, embedded YAML validated with `parseLexicon`, Sync from local API, per-site switches, the opt-in `<all_urls>` permission). `Settings`, `defaultSettings()`, `coerceSettings()`, `siteEnabled()`, `STORAGE_KEYS`.
- Tests: `tests/extension.test.ts` drives both cores under jsdom with a fake background and a mocked `chrome.runtime` (skipped on Node 20).

## apps/macos/LexiconBar (menu bar app)
SwiftPM app, no dependencies, that fronts the CLI; it never links the TypeScript code. `scripts/build-macos-app.sh` builds and ad-hoc signs `apps/macos/build/LexiconBar.app`; the `macos-app` workflow and the release workflow zip it. User docs: `docs/MACOS-APP.md`.
- `LexiconBarKit` (testable, no AppKit): `CLILocator` (finds `lexicon` on PATH, `/opt/homebrew/bin`, `/usr/local/bin`, npm and Homebrew prefixes, or the path saved in Preferences), `CLIOutput` (parses `lexicon voice --json`, `lexicon daemon --once` and `lexicon serve --status --json` output), `RestartPolicy` (5 s backoff, 5 restarts before giving up), `HotKey` (Carbon hotkey encoding for the recorder).
- `LexiconBar` (AppKit): `AppDelegate` builds the status item menu; `HotKeyCenter` registers ⌃⌥Space (push to talk via `lexicon voice --toggle --json`) and ⌃⌥V (`lexicon daemon --once`); `ChildProcessSupervisor` runs `lexicon daemon` and `lexicon serve` as child processes under `RestartPolicy`; `CommandRunner` spawns one-shot CLI calls; `Notifier` posts a notification after each run; `Settings` persists CLI path, whisper model, paste-vs-copy and hotkeys in `UserDefaults`; `PreferencesWindow` is the SwiftUI preferences sheet and `TextWindow` a read-only text window for `lexicon doctor` output.
- Everything the app does is a CLI invocation with the same files, trust gate and hit counters as the terminal, so `CONTRACT.md` for the CLI is the app's contract too.
