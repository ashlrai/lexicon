/node_modules/
dist/
*.log
.DS_Store
coverage/
.lexicon.local.yaml
.claude/launch.json
site/dist/
# real-audio benchmark artifacts (wav, transcripts, json); bench/audio/results.md is committed
bench/audio/out/
bench/audio/models/

# Browser extension build output (npm run build:extension)
extension/dist/
extension/dist-firefox/
extension/*.zip
.scratch/
.vercel
.env*

# Lexicon write safety: the backup writeLexiconFile keeps, and the lock it takes
# across a read-modify-write. Both sit next to the file they protect, so a
# project .lexicon.yaml produces them inside the repo.
*.bak
*.lock
.vitest
